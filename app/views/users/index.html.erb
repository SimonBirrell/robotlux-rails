<%= render partial: 'layouts/index_top', locals: {items_name: 'users', headings: ['Name', 'Email', 'Organization', 'Role']} %>

        <% @users.each do |user| %>
              <%= render user %>
        <% end %>

<%= render partial: 'layouts/index_bottom' %>

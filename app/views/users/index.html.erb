<%= render partial: 'layouts/index_top', locals: {items_name: 'users', item_name: 'user', headings: ['Name', 'Email', 'Organization', 'Role'], new_item: true} %>

        <% @users.each do |user| %>
              <%= render user %>
        <% end %>

<%= render partial: 'layouts/index_bottom' %>
